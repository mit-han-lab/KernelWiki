---
id: pr-flash-attention-2489
repo: Dao-AILab/flash-attention
pr: 2489
title: '[hd256] Add TMA paged KV support to SM100 2CTA forward kernel'
author: Johnsonms
date: '2026-04-23'
url: https://github.com/Dao-AILab/flash-attention/pull/2489
source_category: upstream-code
architectures:
- sm100
architecture_disposition: exact
architecture_evidence:
- architecture: sm100
  basis: exact-sm-token
  locator: PR title
  evidence: SM100
- architecture: sm100
  basis: exact-sm-token
  locator: PR description
  evidence: SM100
- architecture: sm100
  basis: exact-sm-token
  locator: PR description
  evidence: sm100
tags:
- quantization
- tma
techniques: []
hardware_features:
- tma
kernel_types:
- quantization
languages:
- cute-dsl
captured_at: '2026-08-18'
status: merged
merge_sha: cb213fce11c3baf9168f7fa607bc7f22e3323554
inclusion_reason: 'retain: Python DSL device-kernel implementation added in: flash_attn/cute/paged_kv.py,
  flash_attn/cute/sm100_hd256_2cta_fmha_forward.py'
scope_disposition: retained
scope_evidence:
  rule: python-dsl-device-kernel
  paths:
  - flash_attn/cute/paged_kv.py
  - flash_attn/cute/sm100_hd256_2cta_fmha_forward.py
changed_files_count: 4
changed_files_enumerated_count: 4
changed_files_listing_complete: true
changed_paths:
- flash_attn/cute/interface.py
- flash_attn/cute/paged_kv.py
- flash_attn/cute/sm100_hd256_2cta_fmha_forward.py
- tests/cute/test_flash_attn.py
changed_paths_complete: true
body_contract: upstream-pr-v1
upstream_body_locator: https://github.com/Dao-AILab/flash-attention/pull/2489 (PR
  description)
upstream_body_sha256: 3f31fdda160b4c93ca82584ddaea927d6a62510793e3012f18dcc8c7742bf9c6
upstream_excerpt_sha256: d0cd313788c7ff14012ac1b5dc615910ebac5cc05d9d0cb51931f9d1112fca04
upstream_files_sha256: 09b4fab818530ce5304ff55d1c3ae50123121cb70ddea7560a4a8769112309aa
upstream_patches_complete: null
---

## Upstream PR description (verbatim excerpt)

<!-- upstream-excerpt-start -->
## Change

Add paged KV support to the SM100 hd256 2CTA forward kernel.

The paged path reuses the dense TMA load path. Logical KV blocks are remapped to physical page indices through the page table at load time, so each page maps to exactly one TMA tile.

**Constraint:** `page_size` must equal `tile_n = 128`.

### `flash_attn/cute/sm100_hd256_2cta_fmha_forward.py`

- Add conditional K/V tensor layouts in `__call__`:
  - Dense: `(s_k, d, ((h_r, h_k), b))`
  - Paged K: `(page_size, d, h_k, num_pages)`
  - Paged V uses the corresponding transposed layout.
- Add conditional K/V TMA setup in the load warp:
  - Dense path uses `domain_offset` + batch indexing.
  - Paged path slices by `head_kv` and keeps `num_pages` as the outer mode for per-load `page_idx` lookup.
- Add conditional per-load `page_idx` handling:
  - K uses mode-2 subtile + mode-3 page.
  - V uses mode-1 page.
- Plumb `mPageTable` and `max_seqlen_k` through the kernel signature.
- Use `max_seqlen_k` for `seqlen_k` in all four warp sections on the paged path.
- Store `qhead_per_kvhead` on `self` and derive `head_kv_coord` via integer division, matching `flash_fwd_sm100` for contiguous GQA grouping.
- Relax `mPageTable` /
<!-- upstream-excerpt-end -->

## Changed files (upstream)

- `flash_attn/cute/interface.py`
- `flash_attn/cute/paged_kv.py`
- `flash_attn/cute/sm100_hd256_2cta_fmha_forward.py`
- `tests/cute/test_flash_attn.py`
