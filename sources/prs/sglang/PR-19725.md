---
id: pr-sglang-19725
repo: sgl-project/sglang
pr: 19725
title: '[SGLang-Diffusion] Fix custom op fake impl missing eps default for torch.compile'
author: zhaochenyang20
date: '2026-03-03'
url: https://github.com/sgl-project/sglang/pull/19725
source_category: upstream-code
architectures:
- blackwell
- hopper
architecture_disposition: family
architecture_evidence:
- architecture: blackwell
  basis: explicit-family-name
  locator: PR description
  evidence: blackwell
- architecture: hopper
  basis: explicit-family-name
  locator: PR description
  evidence: hopper
tags:
- flash-attention
techniques:
- pipeline-stages
hardware_features: []
kernel_types:
- flash-attention
languages:
- cute-dsl
captured_at: '2026-08-18'
status: merged
merge_sha: 62480ebb1ba4ffc065a0d0df7fbf920c7e42243f
inclusion_reason: 'retain: Python DSL device-kernel implementation added in: python/sglang/jit_kernel/diffusion/cutedsl/scale_residual_norm_scale_shift.py'
scope_disposition: retained
scope_evidence:
  rule: python-dsl-device-kernel
  paths:
  - python/sglang/jit_kernel/diffusion/cutedsl/scale_residual_norm_scale_shift.py
changed_files_count: 1
changed_files_enumerated_count: 1
changed_files_listing_complete: true
changed_paths:
- python/sglang/jit_kernel/diffusion/cutedsl/scale_residual_norm_scale_shift.py
changed_paths_complete: true
body_contract: upstream-pr-v1
upstream_body_locator: https://github.com/sgl-project/sglang/pull/19725 (PR description)
upstream_body_sha256: b4eb686ad0283e2a08faf0e91583f691c54da4f7a40e00c85369e98c7753e89c
upstream_excerpt_sha256: bcdc9316e29cc915e02db744b5a190211d79359ef787cbe46038b4f2a7012906
upstream_files_sha256: ba8e18707a17cddd94ca3ef5492bb627c47e25f9d35c4d6d8661da5a389a09a1
upstream_patches_complete: null
---

## Upstream PR description (verbatim excerpt)

<!-- upstream-excerpt-start -->
## Motivation

While testing #18154 with `--enable-torch-compile`, torch.compile's fake tensor tracing produces repeated `TypeError` traces:

```python
TypeError: fused_scale_residual_norm_scale_shift_fake() missing 1 required positional argument: 'eps'
```

These don't cause hard failures (torch.compile falls back to eager), but they spam the log and hurt debuggability.

## Modifications

Add `eps=1e-5` default to both `register_fake` implementations, matching the `@custom_op` signatures:

- `_fused_norm_scale_shift_fake(..., eps)` → `(..., eps=1e-5)`
- `_fused_scale_residual_norm_scale_shift_fake(..., eps)` → `(..., eps=1e-5)`

## Test Plan

1. Before the PR, running:

```bash
cd python
uv pip install -e ".[diffusion]
# This is required for GLM
pip install --upgrade transformers

sglang serve --model-path zai-org/GLM-Image --enable-torch-compile

# And, sends a request!

curl -sS -X POST "http://localhost:30000/v1/images/generations" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer sk-proj-1234567890" \
  -d '{
        "prompt": "A calico cat playing a piano on stage",
        "size": "1024x1024",
        "n": 1,
        "response_format": "b64_json"
      }'
<!-- upstream-excerpt-end -->

## Changed files (upstream)

- `python/sglang/jit_kernel/diffusion/cutedsl/scale_residual_norm_scale_shift.py`
