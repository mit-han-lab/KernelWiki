---
id: pr-flashinfer-3328
repo: flashinfer-ai/flashinfer
pr: 3328
title: 'feat(cute_dsl/moe): add `moe_output_memset_inplace` dense memset wrapper'
author: leejnau
date: '2026-05-14'
url: https://github.com/flashinfer-ai/flashinfer/pull/3328
source_category: upstream-code
architectures: []
architecture_disposition: unknown
architecture_evidence:
- architecture: unknown
  basis: no-discriminating-architecture-evidence
  locator: PR title, description, paths, and target-signaling additions in 5 changed
    files
  evidence: No qualifying target-architecture evidence survived the deterministic
    context filters.
tags:
- gemm
- moe
techniques: []
hardware_features: []
kernel_types:
- gemm
- moe
languages:
- cute-dsl
captured_at: '2026-08-18'
status: merged
merge_sha: 34fe1ff09b3e157c63df7476cbed344045f94b7c
inclusion_reason: 'retain: Python DSL device-kernel implementation added in: flashinfer/fused_moe/cute_dsl/fused_moe.py,
  flashinfer/fused_moe/cute_dsl/moe_utils.py'
scope_disposition: retained
scope_evidence:
  rule: python-dsl-device-kernel
  paths:
  - flashinfer/fused_moe/cute_dsl/fused_moe.py
  - flashinfer/fused_moe/cute_dsl/moe_utils.py
changed_files_count: 5
changed_files_enumerated_count: 5
changed_files_listing_complete: true
changed_paths:
- csrc/moe_utils_binding.cu
- flashinfer/fused_moe/cute_dsl/fused_moe.py
- flashinfer/fused_moe/cute_dsl/moe_utils.py
- flashinfer/jit/moe_utils.py
- tests/moe/test_cute_dsl_fused_moe.py
changed_paths_complete: true
body_contract: upstream-pr-v1
upstream_body_locator: https://github.com/flashinfer-ai/flashinfer/pull/3328 (PR description)
upstream_body_sha256: 540c2cf61c50a718dbae989af0edda2b412845cd1845c5aa15c0c5149e5f257f
upstream_excerpt_sha256: eb868b55271cc3054d28a4c22033fb432d1bee0cf5abb745d76edd636af012e8
upstream_files_sha256: 203dee5a505021ffded87f4047a31892a21c46885466c89fb32c206d3c755a71
upstream_patches_complete: null
---

## Upstream PR description (verbatim excerpt)

<!-- upstream-excerpt-start -->
## 📌 Description

Adds `moe_output_memset_inplace`, a thin C++ binding around `cudaMemsetAsync` for zeroing the active MoE output slice before GEMM2 finalize, and uses it to replace the two `moe_output.zero_()` call sites in `_moe_core_impl`. Mirrors TRT-LLM's `moe_output_memset_inplace` Path A (`cuteDslMoeUtilsOp.cpp` at the `!enable_alltoall || ep_size <= top_k` branch). Functionally equivalent to `tensor.zero_()` but with lower per-call launch overhead (one `cudaMemsetAsync` vs PyTorch's `FillFunctor<c10::BFloat16>` kernel) — saves ~2-3 µs per call at the cells where memset cost is visible.

**Stream selection**: the C++ binding takes an explicit `cuda_stream_ptr` parameter (PyTorch's current stream); the Python wrapper passes `_get_cuda_stream_ptr()`. This is required because the underlying `get_current_stream()` C++ helper resolves through `TVMFFIEnvGetStream`, not `at::cuda::getCurrentCUDAStream()` — so the Python `torch.cuda.stream(...)` context manager would otherwise not propagate to the `cudaMemsetAsync` call and aux-stream memset overlap with surrounding GEMM work would be silently nullified. Same pattern as `moe_sort` / `flashinfer_moe_sort` in this same file.

**Scope*
<!-- upstream-excerpt-end -->

## Changed files (upstream)

- `csrc/moe_utils_binding.cu`
- `flashinfer/fused_moe/cute_dsl/fused_moe.py`
- `flashinfer/fused_moe/cute_dsl/moe_utils.py`
- `flashinfer/jit/moe_utils.py`
- `tests/moe/test_cute_dsl_fused_moe.py`
