---
id: pr-flashinfer-1483
repo: flashinfer-ai/flashinfer
pr: 1483
title: 'perf: add fast path to TopPRenormProbKernel for top_p >= 1.0, significantly
  boosting SGLang workloads'
author: TianyuZhang1214
date: '2025-08-14'
url: https://github.com/flashinfer-ai/flashinfer/pull/1483
source_category: upstream-code
architectures: []
architecture_disposition: unknown
architecture_evidence:
- architecture: unknown
  basis: no-discriminating-architecture-evidence
  locator: PR title, description, paths, and target-signaling additions in 3 changed
    files
  evidence: No qualifying target-architecture evidence survived the deterministic
    context filters.
tags: []
techniques: []
hardware_features: []
kernel_types: []
languages:
- cuda-cpp
captured_at: '2026-08-18'
status: merged
merge_sha: cba02c4e9858eba08d26f97436e8fb42870492ef
inclusion_reason: 'retain: CUDA/CuTe/PTX device implementation path(s): include/flashinfer/sampling.cuh'
scope_disposition: retained
scope_evidence:
  rule: cuda-cute-ptx-device-source
  paths:
  - include/flashinfer/sampling.cuh
changed_files_count: 3
changed_files_enumerated_count: 3
changed_files_listing_complete: true
changed_paths:
- benchmarks/bench_renorm.py
- include/flashinfer/sampling.cuh
- tests/test_sampling.py
changed_paths_complete: true
body_contract: upstream-pr-v1
upstream_body_locator: https://github.com/flashinfer-ai/flashinfer/pull/1483 (PR description)
upstream_body_sha256: e662dbd41d28201d8bb33a146703b6ab2988ff9550d7886100c752ffe2418088
upstream_excerpt_sha256: b29d2439f22ddffc027ef05c88095c763b7d8223a2604c58a6067903b7677c28
upstream_files_sha256: 7f83f222580c7b16ca86dd4eff8a6365a1a4b5676d7bc5121d3b8b68f506bed7
upstream_patches_complete: null
---

## Upstream PR description (verbatim excerpt)

<!-- upstream-excerpt-start -->
<!-- .github/pull_request_template.md -->

## 📌 Description
### Summary

When deploying **DeepSeek-R1** with **DP+EP+MTP** in SGLang and sending requests with `temperature > 0.0`, we observed a **\~15% performance degradation**.
Profiling revealed that `TopPRenormProbKernel` took about **13 ms**, whereas disabling MTP switches to `TopKTopPSamplingFromProbKernel`, which only costs \~**100 μs**.
By default, SGLang sets `top_p = 1.0`.

---

### Root Cause

Code analysis shows:

* **`TopKTopPSamplingFromProbKernel`**
  Has an early-stop when `top_k > vocab_size` and `top_p >= 1.0`:

  ```cu
  if (aggregate_gt_pivot_0.count < k && aggregate_gt_pivot_0.value < p) {
      // case 1: pivot_0 accepted
      break;
  }
  ```

* **`TopKRenormProbKernel`**
  Has a fast path when `top_k > vocab_size`:

  ```cu
  if (k < d) {
    // do search
  }
  // Write probs directly to output.
  ```

* **`TopPRenormProbKernel`**
  Performs a ternary search until `min_gt_low == max_le_high`, even when `top_p >= 1.0`.
  This makes the search both **time-consuming** and **unnecessary** in such cases.

---

### Optimization

When `top_p >= 1.0`, ternary search can be skipped entirely, and only normalization is
<!-- upstream-excerpt-end -->

## Changed files (upstream)

- `benchmarks/bench_renorm.py`
- `include/flashinfer/sampling.cuh`
- `tests/test_sampling.py`
