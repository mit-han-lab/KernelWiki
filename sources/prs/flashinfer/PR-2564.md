---
id: pr-flashinfer-2564
repo: flashinfer-ai/flashinfer
pr: 2564
title: 'fix: W4A8 autotune crash in cutlass_fused_moe profiler workspace'
author: ccs1112
date: '2026-02-14'
url: https://github.com/flashinfer-ai/flashinfer/pull/2564
source_category: upstream-code
architectures:
- sm90
architecture_disposition: exact
architecture_evidence:
- architecture: sm90
  basis: exact-sm-token
  locator: PR description
  evidence: SM90
- architecture: sm90
  basis: documented-product-mapping
  locator: PR description
  evidence: H100
  mapping_source: https://developer.nvidia.com/cuda/gpus
tags:
- fp4
- fp8
- moe
- quantization
techniques: []
hardware_features:
- fp4
- fp8
kernel_types:
- moe
- quantization
languages:
- cuda-cpp
captured_at: '2026-08-18'
status: merged
merge_sha: 127699c2ca45521f7aa1dd8c3c653a78b363dc6a
inclusion_reason: 'retain: CUDA/CuTe/PTX device implementation path(s): csrc/fused_moe/cutlass_backend/cutlass_fused_moe_kernels.cuh'
scope_disposition: retained
scope_evidence:
  rule: cuda-cute-ptx-device-source
  paths:
  - csrc/fused_moe/cutlass_backend/cutlass_fused_moe_kernels.cuh
changed_files_count: 2
changed_files_enumerated_count: 2
changed_files_listing_complete: true
changed_paths:
- csrc/fused_moe/cutlass_backend/cutlass_fused_moe_kernels.cuh
- tests/moe/test_trtllm_cutlass_fused_moe.py
changed_paths_complete: true
body_contract: upstream-pr-v1
upstream_body_locator: https://github.com/flashinfer-ai/flashinfer/pull/2564 (PR description)
upstream_body_sha256: 1eb4148765d50d7746f8bcdb7fdda676af161298d546ee57cf380f60a4253a78
upstream_excerpt_sha256: 7f75b85350ee9773470a0e806ee03b5bc17c24a3e736150ebdd849d7b3b7ee5f
upstream_files_sha256: 37b737cd2dadcfd9b8576160141dc3b6119b034ee8dedac621dacc8cdb206466
upstream_patches_complete: null
---

## Upstream PR description (verbatim excerpt)

<!-- upstream-excerpt-start -->
## Description

**Problem**

`cutlass_fused_moe` crashes with `RuntimeError: Assertion failed: quant_1 && quant_2` when called inside `autotune()` with W4A8 quantization. Other quant modes (FP8 block scaling, BF16+MXFP4) are unaffected.

**Root Cause**

`getProfilerWorkspaces()` in `cutlass_fused_moe_kernels.cuh` does not recognize `kUINT8` as an integer-quantized weight type. W4A8 uses `kUINT8` (packed INT4 pairs), but the three type-check sites only match `kINT8` and `kINT4`:

1. `is_int_w_quant` — controls per-tensor scale buffer allocation
2. `is_int_groupwise_w_quant` — controls groupwise scale buffer allocation
3. `dtype_bytes` ternary — controls scale factor byte width

With `kUINT8` unrecognized, all three evaluate false, so the profiler allocates zero-size quant buffers. When `prepareQuantParams()` later tries to use them, it asserts on the resulting nullptrs.

The consumer side (`prepareQuantParams`) already handles `kUINT8` correctly — only the allocator side was missing it. Notably, `is_wfp4a16_quant` in the same function *does* check for `kUINT8`, but only when activations are HALF/BF16 — not FP8. So the workspace code was partially aware of `kUINT8`; the gap was speci
<!-- upstream-excerpt-end -->

## Changed files (upstream)

- `csrc/fused_moe/cutlass_backend/cutlass_fused_moe_kernels.cuh`
- `tests/moe/test_trtllm_cutlass_fused_moe.py`
